# Generated by ARFNI CI/CD Setup
name: arfni-test

services:
  spring:
    build: ./apps/springboot/
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    volumes:
      - ./apps/app.jar:/app/app.jar
    command: java -jar /app/app.jar

networks:
  default:
    driver: bridge
